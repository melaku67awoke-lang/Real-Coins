Batch 52 - Referral UI and registration wiring

Implemented:
- Registration page now has optional Referral Code field.
- Local registration stores the supplied referral code.
- Backend registration accepts an optional referralCode and creates a one-time referral relationship.
- Backend assigns each account a unique referral code.
- Added authenticated GET /v1/referral endpoint.
- Added Referral page in Settings.
- Added Dashboard "Referral & Earn" button that opens the Settings Referral page.
- Referral page shows personal code, copy action, referred count, and one-time reward information.
- Room database migrated 11 -> 12 for referral-code storage.

Verification:
- backend/src/index.js: node --check passed.
- Android Gradle build was not run because this ZIP does not contain a gradlew script/Gradle wrapper JAR and no system Gradle is installed in the working environment.
